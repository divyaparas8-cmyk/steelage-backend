// Automated reminder scheduler disabled and deleted per user request.
const startReminderScheduler = () => {
  console.log('[Reminder Scheduler] Automated email/WhatsApp background scheduler is completely disabled.');
};

module.exports = { startReminderScheduler };
