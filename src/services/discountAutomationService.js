// Automated discount scheduler disabled and deleted per user request.
const startDiscountScheduler = () => {
  console.log('[Discount Scheduler] Automated discount scheduler is completely disabled.');
};

module.exports = { startDiscountScheduler };
